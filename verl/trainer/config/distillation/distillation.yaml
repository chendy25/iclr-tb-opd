# Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
_target_: verl.workers.config.DistillationConfig

# specify the default per-component configs
defaults:

  # <folder_name>@<field_name>.<field_name>: <yaml_file_name>

  # load the default config, then apply the fields in the current yaml
  # self config override anything above
  - _self_

# Whether to enable distillation.
enabled: false

# distillation loss config
distillation_loss:

  # # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
  _target_: verl.workers.config.DistillationLossConfig


  # Loss function mode for distillation
  loss_mode: k3

  # If distillation loss requires top-k logits, this is the value of k
  topk: 32

  # Whether to include task rewards alongside distillation loss.
  use_task_rewards: true


  # The coef of the distillation loss when use_task_rewards is true
  distillation_loss_coef: 1.0

  # Optional max clamp value for distillation loss. If null, no clamping is applied.
  loss_max_clamp: null

  # Optional min clamp value for log probabilities for stability, e.g., log q - log p where p or q are very close to zero.
  log_prob_min_clamp: null

  # Whether to incorporate distillation loss as a reward.
  use_policy_gradient: false

  # Name of the policy loss to use when use_policy_gradient is true.
  policy_loss_mode: "vanilla"

  # PPO clipping ratio for policy loss.
  clip_ratio: 0.2

  # Lower bound for PPO clipping ratio.
  clip_ratio_low: 0.2 

  # Upper bound for PPO clipping ratio.
  clip_ratio_high: 0.2


# Number of GPUs per node in the distillation teacher resource pool
n_gpus_per_node: 8

# Number of nodes in the distillation teacher resource pool
nnodes: 0

# multi-teacher configs
teacher_models:

  # Single-teacher config. This entry is popped when other teacher entries are added, so
  # using `teacher_model` as one of several keys silently drops it. For
  # example, the following CLI overrides result in ONLY `teacher_model2` being used:
  #
  # distillation.teacher_models.teacher_model.key=openai/gsm8k
  # distillation.teacher_models.teacher_model.model_path=Qwen/Qwen3-4B
  # +distillation.teacher_models.teacher_model2.key=hiyouga/geometry3k
  # +distillation.teacher_models.teacher_model2.model_path=Qwen/Qwen3-VL-4B-Instruct
  #
  # Instead, give the first teacher a different name:
  #
  # +distillation.teacher_models.teacher_model1.key=openai/gsm8k
  # +distillation.teacher_models.teacher_model1.model_path=Qwen/Qwen3-4B
  # +distillation.teacher_models.teacher_model2.key=hiyouga/geometry3k
  # +distillation.teacher_models.teacher_model2.model_path=Qwen/Qwen3-VL-4B-Instruct
  teacher_model:
    _target_: verl.workers.config.DistillationTeacherModelConfig
    key: null
    model_path: null
    num_replicas: 0
    inference:
      _target_: verl.workers.config.RolloutConfig
      name: ${oc.select:actor_rollout_ref.rollout.name}
      dtype: ${oc.select:actor_rollout_ref.rollout.dtype}
      gpu_memory_utilization: 0.5
      enforce_eager: true
      cudagraph_capture_sizes: null
      free_cache_engine: true
      data_parallel_size: 1
      expert_parallel_size: 1
      tensor_model_parallel_size: 2
      max_num_batched_tokens: 8192
      max_model_len: null
      max_num_seqs: 1024
      load_format: auto
      engine_kwargs: {}
      limit_images: null
      enable_chunked_prefill: true
      enable_prefix_caching: true
      disable_log_stats: true
      skip_tokenizer_init: true
      prompt_length: ${oc.select:actor_rollout_ref.rollout.prompt_length}
      response_length: ${oc.select:actor_rollout_ref.rollout.response_length}
      temperature: ${oc.select:actor_rollout_ref.rollout.temperature}

# Key to route examples to the appropriate teacher model in multi-teacher setups. Should correspond to a field in the data proto, e.g., task.
teacher_key: data_source

# Token-level branching OPD (TB-OPD). Disabled by default -> standard OPD (B1) unaffected.
tb_opd:

  # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
  _target_: verl.workers.config.TBOPDConfig

  # Master switch for token-level branching.
  enable: false

  # Number of alternative tokens expanded into full branch continuations (rollout.n should be 1 + k).
  k: 2

  # Only branch when the main trajectory is judged incorrect; otherwise fill extra slots with plain rollouts.
  # False (the math token arms' setting): the fork point is chosen by uncertainty /
  # disagreement, which is just as informative on a trajectory that happened to land on
  # the right answer.
  only_fail: false

  # Fork uncertainty metric: "entropy" (truncated top-k entropy) or "topk_gap" (top1-top2 margin).
  fork_metric: entropy

  # Number of top-k prompt logprobs requested in the second student forward.
  topk_logprobs: 20

  # Minimum remaining response budget required at a fork position.
  branch_min_tokens: 8

  # Reward score >= threshold counts the main trajectory as correct.
  correct_threshold: 1.0

  # Fork-position selection among filtered candidates: "argmax" or "topk_uniform".
  fork_select: argmax

  # Number of most-uncertain positions kept before uniform sampling (topk_uniform only).
  fork_topk_positions: 20

  # Number of leading response positions to exclude from forking (CURE skips position 0).
  fork_skip_first: 1

  # Skip a candidate unless its decoded token, stripped of whitespace, is longer than this.
  fork_min_token_strip_len: 1

  # Floor on a fork candidate's RAW uncertainty, applied before normalization; below
  # this no fork is emitted and the extra slots degrade to plain rollouts. 0 disables.
  # Used by both fork units (turn unit: units follow the active estimator).
  fork_min_entropy: 0.0

  # Exclude the main-sampled token from the expanded candidate set.
  fork_dedup_main: true

  # Fork-position eligibility filter: "math_aware" (keep digits/operators + words,
  # drop only special/whitespace/pure-punctuation) or "strip_len" (legacy CURE).
  fork_token_filter: math_aware

  # Scheme B: read fork candidates from the main rollout's own top-k logprobs
  # (logprobs=topk_logprobs) instead of a second student forward.
  scheme_b: false

  # Validation only: also run the legacy second forward and log fork-position
  # agreement (tb_opd_schemeb_pos_match). Doubles selection cost; keep off.
  scheme_b_validate: false

  # Branch continuation mode: forced_topk (M*) or resample (B4 / CURE-style).
  branch_mode: forced_topk

  # Sampling temperature for resample branches; <0 reuses rollout.temperature.
  resample_temperature: -1.0

  # --- Turn-level branching (TB-OPD-Turn) ---

  # Fork granularity: "token" (母方法) or "turn" (agentic multi-turn tool-use).
  fork_unit: token

  # Leading assistant tokens of a turn used to compute its uncertainty signal
  # (ARPO's post-tool entropy spike window). <=0 uses the whole turn.
  turn_first_k: 16

  # Only turns immediately following a tool response are eligible fork points (ARPO).
  # Off by default: the token path imposes no positional prior.
  turn_only_post_tool: false

  # Number of leading assistant turns to exclude from forking. 0 mirrors the token
  # path, whose fork_skip_first=1 skips a single token rather than a whole turn.
  turn_skip_first: 0

  # Budget B: max number of turns forked per trajectory (Phase 1' uses 1).
  max_branches_per_traj: 1

  # Mask a branch's replay of the main trajectory out of its loss, so the shared prefix
  # is trained once instead of (1 + k) times.
  dedup_shared_prefix: true

  # Weighting of the k+1 continuations of a fork: "off" (uniform) or "rb"
  # (Rao-Blackwell: proportional to the student's own probability of the forced token).
  # Uniform averaging over forced top-k alternatives is off-policy.
  # Requires branch_mode=forced_topk; the resample ablation must set this back to "off".
  branch_weight_mode: "rb"

  # Temperature on the RB weights; 1.0 = exact, larger -> uniform.
  branch_weight_temp: 1.0

  # Minimum per-slot RB weight before renormalization; 0.0 = exact RB.
  branch_weight_floor: 0.0

  # (Retired: the turn unit now gates raw uncertainty via the shared fork_min_entropy
  # above. A floor on the *normalized* signal could never fire -- both rank and min-max
  # map the best candidate to 1.0.)

  # AEPO-style penalty on consecutive high-signal turns to curb over-branching.
  consecutive_high_entropy_penalty: false

  # Penalty multiplier applied when the preceding turn is also high-signal.
  consecutive_penalty_weight: 0.5

  # --- Shared fork scoring axes (apply to both fork units) ---

  # Where a turn fork may land: post_tool | turn_open | reasoning | action | all.
  # null derives it from turn_only_post_tool. reasoning/action need per-turn
  # action spans from the agent loop (ALFWorld/tool_agent record them).
  fork_eligibility: null

  # Weight on the uncertainty term: score = alpha*U + (1-alpha)*D for fuse=blend.
  # 1.0 is pure uncertainty (no teacher needed); <1 pulls the teacher forward.
  # 0.5 = the even blend the math token arms run (the "klfork" arms).
  fork_alpha: 0.5

  # How U and D combine: blend | max | union | soft_or (ATOD). Everything except
  # blend with alpha=1 requires a teacher signal.
  fork_fuse: blend

  # Forward window (tokens) for the signed teacher/student disagreement D.
  fork_kl_window: 128

  # Per-trajectory normalization before fusing: rank (token path) or minmax (ATOD).
  fork_normalize: rank

  # Signed D = logp_student - logp_teacher (isolates student over-confidence).
  # false uses the unsigned |delta| of the original ATOD formulation.
  disagreement_signed: true

  # Minimum turn distance between two fork points when max_branches_per_traj > 1.
  fork_min_turn_gap: 1

  # --- Turn-level loss reweighting (B-A1, reweight-only; no branch expansion) ---

  # Emphasize KD loss on high-uncertainty assistant turns without forking.
  # Independent of `enable`; entropy-only (no teacher-at-rollout disagreement).
  turn_reweight: false

  # Emphasis strength: per-token weight = 1 + reweight_alpha * normalized_signal.
  reweight_alpha: 1.0

  # Per-turn signal for reweighting: "ent" (mean -logp) or "dHtool" (post-tool increase).
  reweight_metric: ent